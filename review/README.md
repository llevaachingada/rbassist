# Review bin (local only)

Drop questionable/old files here while cleaning up the repo.

Everything in this folder is ignored by Git (except this README and `.gitignore`), so you can sort through it later without pushing it to GitHub.
